# Update Streamlit pages


This project uses **GitHub Actions + Docker + Puppeteer** to keep a web application active by periodically sending an automated request.

The main goal is to prevent hibernation of an application hosted on platforms such as Streamlit Cloud.
